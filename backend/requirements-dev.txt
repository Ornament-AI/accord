# Direct dependency pins for development, tests, and repository tooling.
# The production Dockerfile reads only requirements.txt when installing Python
# packages; some packages listed here may still be installed transitively.

# Sync Postgres driver used by database integration tests.
# The application itself talks to Postgres via asyncpg.
psycopg[binary]==3.3.4

# HTTP client used by backend tests and repository seed scripts.
httpx==0.28.1

# Backend test runner and async test support.
pytest==9.1.1
pytest-asyncio==1.4.0

# Backend linting. Kept in dev deps so production images stay lean.
ruff==0.15.20

# Property-based testing for domain money/rate primitives.
hypothesis==6.156.6

# PDF text extraction for report formatter tests only.
pypdf==6.14.2
