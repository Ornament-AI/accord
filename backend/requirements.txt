# Core web framework
fastapi[standard]==0.139.0
uvicorn[standard]==0.50.0
# Intentional direct pin for FastAPI's standard extra; multipart parser upgrades stay explicit.
python-multipart==0.0.32

# Database and ORM
sqlmodel==0.0.39
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5

# Configuration and serialization
pydantic-settings==2.14.2
structlog==26.1.0

# Rate limiting
slowapi==0.1.10

# Auth (WorkOS identity + signed session cookies)
workos==10.2.0
itsdangerous==2.2.0

# Object storage (S3-compatible)
boto3==1.43.51

# Report export (Excel / PDF)
openpyxl==3.1.5
fpdf2==2.8.7

# Observability
prometheus-client==0.25.0
